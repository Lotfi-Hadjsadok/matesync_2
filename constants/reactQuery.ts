/** Boards, lists, and cards — poll so partner edits show up without manual refresh */
export const REFETCH_BOARD_MS = 10_000

/** Profile / couple (e.g. partner joined invite) */
export const REFETCH_PROFILE_MS = 20_000
